digraph asd {
    rankdir=LR;
    Buttons -> micro
    Pots -> micro
    MIDI -> micro
    micro -> FPGA -> DAC -> minijack

    edge [color=red]
    Buttons -> FPGA -> minijack
    MIDI -> FPGA
    micro -> DAC
    FPGA -> micro
}
